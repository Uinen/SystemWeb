@{
    ViewBag.Title = "Operazione non Permessa";
    ViewBag.Description = "Spiacenti, ma il metodo utilizzato per contattare il server non è stato consentito";
}

<div class="jumbotron text-center">
    <header>
        <h1><span aria-hidden="true" class="fa fa-ban text-danger"></span> @ViewBag.Title</h1>
    </header>
    <p>@ViewBag.Description</p>
    <a class="btn btn-primary btn-lg" href="@Url.RouteUrl(HomeControllerRoute.GetIndex)"><span aria-hidden="true" class="fa fa-home"></span> Torna indietro</a>
</div>